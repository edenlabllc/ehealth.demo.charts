apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-mongo-primary-shard1
spec:
  storageClassName: local-storage
  capacity:
    storage: 1200Gi
  accessModes:
  - ReadWriteOnce
  hostPath:
    path: "/mongodb-2"
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - k8s-me-0