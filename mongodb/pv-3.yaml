apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-mongo-primary
spec:
  storageClassName: local-storage
  capacity:
    storage: 2000Gi
  accessModes:
  - ReadWriteOnce
  hostPath:
    path: "/mongodb"
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - gke-cluster-1-denovo-me-d7d0abf2-0vrg