apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-kafka-1
spec:
  storageClassName: local-storage
  capacity:
    storage: 40Gi
  accessModes:
  - ReadWriteOnce
  hostPath:
    path: "/kafka-1/kafka"
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - k8s-kafka-1