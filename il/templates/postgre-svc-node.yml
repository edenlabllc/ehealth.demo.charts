apiVersion: v1
kind: Service
metadata:
  labels:
    app: postgresql
  name: db-svc-nodeport
  namespace: il
spec:
  ports:
  - nodePort: 30013
    port: 30013
    protocol: TCP
    targetPort: 5432
  selector:
    app: postgresql
  #sessionAffinity: None
  type: NodePort
