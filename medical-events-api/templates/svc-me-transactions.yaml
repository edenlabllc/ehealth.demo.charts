apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image_me_transactions.name }}
  labels:
    app: {{ .Values.image_me_transactions.name }}
spec:
  ports:
  - port: 80
    targetPort: 4000
    protocol: TCP
    name: http
  selector:
    app: {{ .Values.image_me_transactions.name }}
  type: ClusterIP
